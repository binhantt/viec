
// initial state
const state = {
   
};

// getters lay
const getters = {
  //  allTodos: (state) => state.todos,

};

// actions doi 
const actions = {
    async fetchTodos({ commit }) {
        
    },
 
};

// commit=>mutations
const mutations = {
    //setTodos: (state, todos) => state.todos = todos,
 
};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}